spring:
  application:
    name: client-feign

server:
  port: 10003
  servlet:
    context-path: /cloud


eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
    fetch-registry: true
    register-with-eureka: true
